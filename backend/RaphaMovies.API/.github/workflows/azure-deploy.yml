name: Build and Deploy Backend

on:
  push:
    branches:
      - main
      - fix/efcore-and-swagger

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        working-directory: backend/RaphaMovies.API
        run: dotnet restore

      - name: Build
        working-directory: backend/RaphaMovies.API
        run: dotnet build --configuration Release --no-restore

      - name: Publish
        working-directory: backend/RaphaMovies.API
        run: dotnet publish --configuration Release --no-build -o ${{ github.workspace }}/publish

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-publish
          path: ${{ github.workspace }}/publish

      # Uncomment and configure the step below to deploy to Azure WebApp
      # - name: Deploy to Azure WebApp
      #   uses: azure/webapps-deploy@v4
      #   with:
      #     app-name: <YOUR_WEBAPP_NAME>
      #     publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
      #     package: ${{ github.workspace }}/publish
